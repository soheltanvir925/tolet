name: Deploy this webiste to GitHub Pages
on:
  push:
    branches:
      -main


jobs:
  test:
    runs-on: ubuntu-latest
      steps:
      - name: Checkout repository
        uses: actions/checkout@v4 
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - name: Run tests
        run: echo "No tests yet"
  
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
         
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'
        
      - name: Build the site
        run: echo "No build step required"  

